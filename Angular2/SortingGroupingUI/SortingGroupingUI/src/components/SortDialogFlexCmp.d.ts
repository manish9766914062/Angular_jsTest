import { SortManager } from '../sortManager';
import { ManagerSvc } from '../services/ManagerSvc';
export declare class SortDialogFlexCmp {
    sortManager: SortManager;
    constructor(managerSvc: ManagerSvc);
}
